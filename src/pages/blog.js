import React from "react"
import PageLayout from '../components/pageLayout'

const BlogPage = (props) => {
  return <PageLayout pageName="BLOG" voronoiClipData={props.voronoiClipData["/blog"]}>
      LISTEN
  </PageLayout>
}

export default BlogPage 